import 'package:flutter/material.dart';

class TubeView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tube'),
      ),
      body: Center(
        child: Text('Tube Page Content'),
      ),
    );
  }
}
